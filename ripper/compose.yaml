services:
  filebot:
    image: jlesage/filebot:latest
    container_name: filebot
    ports:
      - 5800:5800
    volumes:
      - ./filebot/:/config:rw
      - /mnt/HDD1/media:/storage:rw
    restart: unless-stopped
